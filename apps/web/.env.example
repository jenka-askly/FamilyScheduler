# Optional for local dev (defaults to relative /api with Vite proxy to localhost:7071)
# VITE_API_BASE_URL=http://localhost:7071

# Required for production/static deployments (set to deployed Function App host)
# Example:
# VITE_API_BASE_URL=https://familyscheduler-api-prod.azurewebsites.net
