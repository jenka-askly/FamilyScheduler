# scripts

Placeholder script directory for local developer automation.

## Planned scripts (documented placeholders)

- `scripts/dev.sh` (placeholder): run web + api in local development mode
- `scripts/lint.sh` (placeholder): lint workspace packages/apps
- `scripts/typecheck.sh` (placeholder): run TypeScript checks
- `scripts/test.sh` (placeholder): run test suites
- `scripts/init-local-state.sh` (placeholder): create initial local state file at `LOCAL_STATE_PATH`

These scripts are not implemented in this scaffold-only phase.
